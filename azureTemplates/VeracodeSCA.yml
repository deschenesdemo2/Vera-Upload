# VeracodeSCA.yml file

parameters:
# SRCCLR token to run Agent-based scan
- name: 'SRCCLR_API_TOKEN'
  type: string
  default: ''

steps:
  - script: echo '[INFO] Starting Veracode SCA - Agent-Based Scan'

  - task: CmdLine@2
    displayName: 'SCA - Agent-Based Scan'
    inputs:
      script: |
        echo 'Probando el directorio del pipeline.'
        ls -all
